numpy>=1.23.5,<2
# For CSRT/KCF trackers (opencv-contrib); the code falls back to MIL if unavailable.
opencv-contrib-python>=4.8,<4.11
# Albumentations pulls this transitively; keep it NumPy 1.x-compatible too.
opencv-python-headless>=4.9,<4.11
pyyaml>=6,<7
tqdm>=4.64,<5
insightface>=0.7.3
onnxruntime>=1.16
# If you have NVIDIA GPU, install this instead of onnxruntime:
# onnxruntime-gpu>=1.16

# YOLO (person presence)
ultralytics>=8.2,<9
torch>=2.1,<3

requests>=2.31,<3
rich>=13,<14

fastapi
uvicorn[standard]

